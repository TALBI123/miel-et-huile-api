export enum ROLE {
  USER = "USER",
  ADMIN = "ADMIN",
}
export enum Type {
  number = "number",
  string = "string",
}
export enum VerificationTokenType {
  EMAIL_VERIFICATION = "EMAIL_VERIFICATION",
  PASSWORD_RESET = "PASSWORD_RESET",
}
